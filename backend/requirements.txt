fastapi==0.115.12
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
pydantic==2.11.4
pydantic-settings==2.9.1
email-validator==2.2.0
PyJWT==2.10.1
bcrypt==4.0.1
google-genai==1.7.0
APScheduler==3.11.2
cryptography==44.0.2
# python:3.12-slim (our Docker base) has no system IANA timezone database, so stdlib
# zoneinfo can't resolve real tz names (e.g. "Asia/Kolkata") without this — needed for the
# business-hours SLA calendar. Windows dev machines have the same gap.
tzdata==2025.2
alembic==1.13.1
